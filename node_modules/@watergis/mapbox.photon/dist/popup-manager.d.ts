import { Map as MapboxMap } from 'mapbox-gl';
export declare type PopupOptions = {
    popupZoomLevel?: number;
    createContent?: Function;
};
export default class PopupManager {
    private map;
    private marker;
    private options;
    constructor(map: MapboxMap, options?: PopupOptions);
    _createContent(feature: GeoJSON.Feature): string;
    createContent(feature: GeoJSON.Feature): any;
    add(feature: GeoJSON.Feature): void;
    clear(): void;
}
