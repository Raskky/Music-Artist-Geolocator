export declare type SearchControlOptions = {
    placeholder?: string;
    minChar?: number;
    submitDelay?: number;
    noResultLabel?: string;
    feedbackUrl?: string;
    feedbackLabel?: string;
    width?: number;
    onSelected?: Function;
    doSearch?: Function;
};
export declare type Choice = {
    feature: GeoJSON.Feature;
    el: HTMLLIElement;
};
export default class SearchControl {
    private input;
    private resultsContainer;
    private options;
    private submitDelay;
    private CACHE;
    private RESULTS;
    private _CURRENT;
    private get CURRENT();
    private set CURRENT(value);
    private KEYS;
    constructor(input: HTMLInputElement, options: SearchControlOptions);
    initialize(): void;
    private createResultsContainer;
    resizeContainer(): void;
    private onKeyDown;
    private onInput;
    private onBlur;
    private onFocus;
    private clear;
    private hide;
    private setChoice;
    private search;
    private _doSearch;
    private _onSelected;
    private onSelected;
    private formatResult;
    private formatType;
    private createResult;
    private onMouseover;
    private onMousedown;
    private resultToIndex;
    private handleResult;
    private highlight;
    private getLeft;
    private getTop;
}
