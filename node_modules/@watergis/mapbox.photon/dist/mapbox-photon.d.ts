import { IControl, Map as MapboxMap } from 'mapbox-gl';
import { SearchControlOptions } from './search-control';
import { GeocodingOptions } from './request';
import { PopupOptions } from './popup-manager';
export default class MapboxPhotonGeocoder implements IControl {
    private controlContainer;
    private map;
    private searchBox;
    private searchControl;
    private photonGeocoding;
    private popupManager;
    private searchControlOptions;
    private geocodingOptions;
    private popupOptions;
    constructor(searchControlOptions?: SearchControlOptions, geocodingOptions?: GeocodingOptions, popupOptions?: PopupOptions);
    getDefaultPosition(): string;
    onAdd(map: MapboxMap): HTMLElement;
    private onSelected;
    onRemove(): void;
}
