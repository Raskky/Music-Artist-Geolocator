export declare type ReverseOptions = {
    url?: string;
    limit?: number;
    osm_tag?: string;
    lang?: string;
};
export default class PhotonReverse {
    private options;
    constructor(options: ReverseOptions);
    reverse(lon: number, lat: number): Promise<import("geojson").FeatureCollection<import("geojson").Geometry, import("geojson").GeoJsonProperties>>;
}
